meta {
  name: create requirement
  type: http
  seq: 1
}

post {
  url: {{gw}}/{{path}}/create
  body: json
  auth: inherit
}

body:json {
  {
    "title": "{{$randomWords}}",
    "description": "Test Description",
    "scopes": "scope1,scope2",
    "tags": "tag1,tag2",
    "references": "http: //example.com"
  }
}

assert {
  res.body.uuid: isString
  res.body.statusCode: eq "201"
  res.body.statusMsg: eq "Requirement created"
}

tests {
  bru.setVar("reqId", res.body.uuid);
  
}

settings {
  encodeUrl: true
}
